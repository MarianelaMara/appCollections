<h1>Makers</h1>
<% if @makers.nil? %>
  <p>Hasta el momento no se recibió respuesta de los lugares de fabricación  solicitados, vuelva a consultar mas tarde. </p>
<% elsif @makers.empty? %>
  <p>No hay lugares disponibles.</p>
  <p>Vuelva a hacer la consulta.</p>
<% else %>
  <%= form_with model: @booking, url: create_bookings_makers_path, method: :post do |form| %>
    <%= hidden_field_tag :collection_id, @collection.id%>
  <table class="table table-striped table-hover table-bordered">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>País</th>
        <th>Dirección</th>
        <th>Ciudad</th>
        <th>Precio</th>
      </tr>
    </thead>
    <tbody>
    <% @makers.each do |maker| %>
      <tr>
        <td><%= maker["name"] %></td>
        <td><%= maker["country"] %></td>
        <td><%= maker["address"] %></td>
        <td><%= maker["city"] %></td>
        <td><%= maker["price"] %></td>
        <td><%= check_box_tag "selected_makers []", maker.to_json%></td>
      </tr>
    <% end %>
    </tbody>
  </table>
  <%= form.submit "Reservar Lugar" %> 
  <% end %>
<% end %>
