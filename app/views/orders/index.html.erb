<p style="color: green"><%= notice %></p>

<h1 class="display-4 text-center text-gray-800"> <%= "Pedidos" %></h1>

<div id="orders" class="container mt-4">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th><%= "Cliente" %></th>
        <th><%= "Collección" %></th>
        <th><%= "Fecha de entrega" %></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @orders .each do |order| %>
        <tr>
          <td><%= order.customer %></td>
          <td><%= order.collection_id %> </td>
          <td><%= order.date_delivery %></td>
          <td><%= link_to "Ver pedido", order, class: "btn btn-primary" %>  </td>
        </tr>
      <% end %>
    </tbody>
  </table>  
</div>